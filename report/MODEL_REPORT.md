# Model report — pending official training run

| Field | Record before submission |
|---|---|
| Task | Crop-disease image classification; exact organizer class count and labels. |
| Dataset and split | Official source/licence; exact train/validation counts; external training data with citations. State that the held-out field set was never used for training/tuning. |
| Model | Backbone, input size, augmentation, optimizer, learning rate, epochs, seed, class weighting/focal loss and calibration method. |
| Metrics | Macro-F1 (primary), accuracy, per-class precision/recall, raw and normalised confusion matrices. Only include measured values. |
| Baseline | Organizer baseline and comparable measured result. |
| Limitations | Domain shift, visual ambiguity, poor-quality images, and low-confidence policy. |

No metrics are recorded yet because the official dataset and trained artifact are unavailable.

